{
  "schema_version": "2.0",
  "name": "ReDoS-prone catastrophic regex",
  "output_mode": "boolean",
  "selector": {"kind": "ifc_class", "values": ["IfcWall"]},
  "predicate": {
    "kind": "triplet",
    "attribute": {"kind": "regex", "pset_name": null, "pattern": "(a+)+b", "case_sensitive": false},
    "constraint": {"operator": "exists"}
  },
  "_invalid_reason": "regex pattern is exponentially backtracking — must be rejected by validator"
}
