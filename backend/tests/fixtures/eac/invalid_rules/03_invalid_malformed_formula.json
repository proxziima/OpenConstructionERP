{
  "schema_version": "2.0",
  "name": "Malformed formula — unmatched parenthesis",
  "output_mode": "aggregate",
  "result_unit": "m3",
  "selector": {"kind": "ifc_class", "values": ["IfcWall"]},
  "formula": "Volume * (Density + ",
  "_invalid_reason": "formula does not parse via simpleeval AST"
}
