{
  "schema_version": "2.0",
  "name": "Unknown attribute kind",
  "output_mode": "boolean",
  "selector": {"kind": "ifc_class", "values": ["IfcWall"]},
  "predicate": {
    "kind": "triplet",
    "attribute": {"kind": "fuzzy", "name": "Mark"},
    "constraint": {"operator": "exists"}
  },
  "_invalid_reason": "attribute.kind 'fuzzy' not in (exact|alias|regex)"
}
