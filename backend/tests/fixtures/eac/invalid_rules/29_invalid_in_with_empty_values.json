{
  "schema_version": "2.0",
  "name": "in operator with empty values list",
  "output_mode": "boolean",
  "selector": {"kind": "ifc_class", "values": ["IfcWall"]},
  "predicate": {
    "kind": "triplet",
    "attribute": {"kind": "exact", "name": "Mark"},
    "constraint": {"operator": "in", "values": []}
  },
  "_invalid_reason": "in.values minItems=1; empty list forbidden"
}
